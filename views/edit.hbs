<head>
  <style>
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
      background-color: #f0f0f0;
      font-family: Arial, sans-serif;
    }

    .styledForm {
      display: flex;
      flex-direction: column;
      width: 300px;
    }

    .styledForm label {
      font-weight: bold;
      margin-top: 10px;
    }

    .styledForm input[type="text"],
    .styledForm textarea {
      padding: 5px;
      margin-top: 5px;
      border: 1px solid grey;
      border-radius: 4px;
    }
    
    .styledForm textarea {
      height: 150px;
    }
    
    .styledForm input[type="submit"] {
      color: white;
      background-color: green;
      border: none;
      border-radius: 4px;
      padding: 10px;
      margin-top: 10px;
      cursor: pointer;
    }

    .styledForm input[type="submit"]:hover {
      background-color: darkred;
    }
  </style>
</head>
<body>
  <form action="/blog/{{post._id}}?_method=PUT" method="POST" class="styledForm">
    <label for="title">제목:</label>
    <input type="text" id="title" name="title" value="{{post.title}}" required>
    <label for="name">작성자:</label>
    <input type="text" id="name" name="name" value="{{post.name}}" required>
    <label for="content">내용:</label>
    <textarea id="content" name="content" required>{{post.content}}</textarea>
    <input type="submit" value="제출">
  </form>
</body>
