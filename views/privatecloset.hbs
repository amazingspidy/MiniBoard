<!DOCTYPE html>
<html>
<head>
  <title>나의 옷장</title>
  <style>
    .grid-container {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 20px;
    }

    .item-box {
      border: 2px solid brown;
      background-color: #f5f5dc;
      margin-bottom: 20px;
      padding: 10px;
      border-radius: 10px;
    }

    .flex-container {
      display: flex;
      flex-direction: row;
    }
    
    .item-info {
      flex: 1;
    }

    .item-info p {
      font-size: 18px; /* 이 부분을 추가하여 글씨 크기를 키웠습니다. */
    }

    .item-image {
      flex: 3;
    }

    .item-image img {
      width: 100%;
      height: auto;
    }

    h1 {
      color: #3c3c3c; /* 텍스트 색상 설정 */
      border-bottom: 2px solid #3c3c3c; /* 테두리 설정 */
      padding-bottom: 10px; /* 패딩 설정 */
      text-shadow: 2px 2px 4px #999999; /* 텍스트 그림자 설정 */
    }
    
  </style>
</head>
<body>
  <h1>{{writer}}님의 옷장</h1>
  <button onclick="window.location.href='/closet'">뒤로가기</button>
  <div class="grid-container">
    {{#each closet as |item|}}
    <div class="item-box">
      <div class="flex-container">
        <div class="item-info">
          <p><strong style = "font-size: 80%;">아이템 이름:</strong> <br>{{item.item_name}}</p>
          <p><strong style = "font-size: 80%;">특징:</strong><br> {{item.point}}</p>
          <p><strong style = "font-size: 80%;">스펙:</strong><br> {{item.spec}}</p>
        </div>
        <div class="item-image">
          <p><strong>실제 이미지:</strong> <br> <img src="{{item.actual_img}}" alt="실제 이미지"></p>
        </div>
      </div>
      <form action="/closet/file-upload" method="post" enctype="multipart/form-data">
        <input type="file" name="file"/>
        <input type="hidden" name="item_id" value="{{item._id}}">
        <input type="hidden" name="writer" value="{{item.writer}}">
        <input type="submit" value="Upload"/>
      </form>
    </div>
    {{/each}}
  </div>
</body>
</html>
