<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>모두의 옷장</title>
    <!-- 여기에 필요한 CSS나 JavaScript 파일을 연결하세요. -->
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;700&display=swap');

        body {
            font-family: 'Noto Sans KR', sans-serif;
        }

        /* 기존의 스타일은 유지하고 추가적으로 아래의 스타일을 적용합니다. */
        ul {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-around;
            padding: 0;
        }

        li {
            list-style: none;
            margin: 10px;
            background: #f8f9fa;
            border-radius: 10px;
            width: 200px;
            height: 200px;
            display: flex;
            justify-content: center;
            align-items: center;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.15);
            transition: all 0.3s ease;
        }

        li:hover {
            transform: scale(1.05);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        }
        button {
            background: none;
            border: none;
            font-size: 1.2em; /* 텍스트 크기를 키웠습니다. */
            font-weight: bold;
            color: #333;
        }
        h1 {
            color: #0000FF;
            
            text-shadow: 1px 1px 0 #1E90FF, 2px 2px 0 #1E90FF, 3px 3px 0 #1E90FF, 4px 4px 0 #1E90FF, 5px 5px 0 #1E90FF;
            position: relative;
            animation: h1move 3s infinite;
        }

        @keyframes h1move {
            0% { transform: rotate(0deg); }
            25% { transform: rotate(5deg); }
            50% { transform: rotate(0deg); }
            75% { transform: rotate(-5deg); }
            100% { transform: rotate(0deg); }
        }


        .highlight {
            color: #c6f08f;
            font-weight: bold;
            text-decoration: underline;
        }
        .back-button {
            background-color: #7878c9;
            color: white;
            border: none;
            padding: 15px 32px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
            transition-duration: 0.4s;
            cursor: pointer;
        }

        .back-button:hover {
            background-color: #4CAF50;
            color: white;
        }
        img {
      width: 100px; /* 이미지 너비를 100px로 설정 */
      height: auto; /* 높이는 자동으로 설정하여 비율을 유지 */
    }
    </style>
</head>
<body style="font-family: 'Noto Sans KR', sans-serif;">
    <!-- 기존의 HTML 코드는 유지하고 추가적으로 아래의 코드를 적용합니다. -->
    <main>
        <section>
            <button class="back-button" onclick="window.location.href='/auth/success'">뒤로가기</button>
            <button class="back-button" onclick="window.location.href='/closet/createcloset'">👚 나의옷장에 옷 추가하기</button>
            <h2>의류 목록 미리보기</h2>

            <ul>
                {{#each closets}}
                <li>
                    <button style="font-size: 12px;" >
                        {{this.writer}} 님의<br> {{this.item_name }}
                    </button>
                    <br><img src="{{this.actual_img}}" alt="실제 이미지">
                </li>
                {{/each}}
            </ul>
        </section>

        <section style="display: flex; flex-direction: column; align-items: center; margin-top: 50px;">
            <h1 style="margin-bottom: 200px; font-size: 300%;">다른 사람들의 옷장을 구경해보세요</h1>

            <ul style="list-style: none;">
                {{#each writers}}
                <li style="margin-bottom: 20px;">
                    <div style="width: 600px; height: 500px; background: #8B4513; border: 8px solid #A0522D; border-radius: 8px; position: relative; box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);">
                        <div style="width: 80%; height: 60px; background: #A0522D; position: absolute; bottom: 0; left: 10%; border-bottom-left-radius: 8px; border-bottom-right-radius: 8px;"></div>
                        <div style="width: 80%; height: 50px; background: #A0522D; position: absolute; top: 10%; left: 10%; border-radius: 5px;"></div>
                        <button style="position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); background: none; border: none; color: white; font-weight: bold; font-size: 20px;" onclick="window.location.href='/closet/{{this}}'">
                            <span class="highlight">{{this}}님의 옷장</span>
                        </button>
                    </div>
                </li>
                {{/each}}
            </ul>
        </section>
    </main>
</body>
</html>
